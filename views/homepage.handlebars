
{{#each movies as |movie|}}
<div class="box">

  <div class="columns is-mobile is-centered is-vcentered">
    <div class="column">
      <img class="image" src={{movie.image_name}} alt="">
    </div>
    
    <div class="column is-flex-direction-row">
      <span class="title">{{movie.movie_name}}</span><br>
      <span class="subtitle">{{movie.movie_description}}</span><br>
      {{#if ../logged_in }} 
        <button class="button like-button has-background-success-light">Like
          <input type="hidden" name="like-id" value="{{movie.id}}" />
        </button>
       {{/if}}
    </div>
  </div>
</div>
{{/each}}

<script src="./js/like.js"></script>